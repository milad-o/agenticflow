"""DEPRECATED: Provider factory functions have been moved to examples/utils/provider_factory.py

The core framework remains provider-agnostic. Use the example factory in dev only.
"""

raise RuntimeError(
    "Provider factories are dev-only and not part of the framework. "
    "Please import from examples.utils.provider_factory instead."
)
